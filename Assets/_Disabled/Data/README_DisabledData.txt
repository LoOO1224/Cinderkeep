Cinderkeep disabled game data

This folder stores old Play Our Game data-driven files that should not be loaded by the current blank rebuild.

Rules for teammates:
- Files ending with .disabled are reference-only backups.
- Do not rename these back into active Assets unless the team lead approves it.
- New gameplay JSON or Excel schemas should be created in a fresh active data folder after the new script structure is agreed.
- Existing hub/workspace data is not managed here.
